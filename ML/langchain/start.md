I installed this by means of a virtual env

    virtualenv langchain
    cd langchain
    source bin/activate

I think these are all packages:

    pip install langchain langchain-chroma langchain-community langchain-ollama jupyter
